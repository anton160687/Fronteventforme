export type filterParamsType = {
    city: string,
    capacity: string[],
    scheme_of_payment: string[],
    price: string[],
    territory: string[],
    more: string[],
    additional: string[],
}