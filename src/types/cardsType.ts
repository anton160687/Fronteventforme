export type CardsType = {
  title: string,
  description: string,
  pathImg: string
}

export type cardConCat = {
  title: string,
  description: string,
  nameImg: string,
  color: string,
}

export type providerCards = {
  id: number,
  title: string,
  description: string[],
  pathImg: string[],
}